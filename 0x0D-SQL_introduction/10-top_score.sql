-- Retrieves records from the table second_table, displaying scores and corresponding names.
-- The results are sorted in descending order based on the score.
SELECT score, name
FROM second_table
ORDER BY score DESC;
