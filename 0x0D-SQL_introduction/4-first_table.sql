-- Establishes a table named first_table with columns id (integer) and name (string, maximum length 256) in the MySQL server, ensuring it is created if not already present.
CREATE TABLE IF NOT EXISTS first_table (
id INT,
name VARCHAR(256)
);






